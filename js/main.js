/* ============================================================
   MAIN — bootstrap all modules
   ============================================================ */
document.addEventListener('DOMContentLoaded', function() {
  initCursor();
  initNav();
  initWwd();
  initPackages();
  initFaq();
  initReveal();
});
